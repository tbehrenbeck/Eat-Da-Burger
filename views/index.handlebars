<h1>Burgers!</h1>

<ul>
  {{#each burger}}
   <li>
       <p>
           <span class= "label">ID:</span>{{this.id}}
       </p>
       <p>
           <span class= "label">Name:</span>{{this.burger_name}}
       </p>
   </li>
  {{/each}}
</ul>
