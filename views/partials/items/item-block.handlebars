<li>
    {{name}}

    <button class="change-need" data-id="{{id}}" data-newNeed="{{need}}">
        {{#if need}}Bought!{{else}}Need{{/if}}
    </button>

    <button class="delete-item" data-id="{{this.id}}">Delete</button>
</li>